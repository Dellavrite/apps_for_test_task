apiVersion: v1
kind: Service
metadata:
  name: database
  namespace: data
spec:
  type: {{ .Values.service.type }}
  ports:
  - port: {{ .Values.service.port }}
    targetPort: 5432
  selector:
    app: database
